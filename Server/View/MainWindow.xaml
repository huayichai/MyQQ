<Window x:Class="Server.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Server"
        mc:Ignorable="d"
        Title="QQServer" Height="450" Width="800" WindowStartupLocation="CenterScreen">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition></RowDefinition>
            <RowDefinition Height="40"></RowDefinition>
        </Grid.RowDefinitions>
        <TextBox x:Name="LogBox" TextWrapping="Wrap" VerticalScrollBarVisibility="Visible"
                 FontSize="20" IsReadOnly="True"></TextBox>
        <StackPanel Grid.Row="1" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button HorizontalAlignment="Right" Margin="0,0,20,0" Width="60" Height="35" Content="用户管理"
                    Click="UserManageClick"></Button>
            <Button  HorizontalAlignment="Right" Width="60" Height="35" Click="MessageManagementClick"
                Margin="0,0,20,0" Content="消息管理"></Button>
        </StackPanel>
    </Grid>
</Window>
